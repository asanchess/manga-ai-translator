# Manga AI Translator — Lean Agent System & Project Guidelines

Этот проект управляется гибридной командой из трех ролей, оптимизированных под Google AI Pro ($20/мес) и специфику обработки манги/вебтунов.

## 👥 Роли команды

1. 🧠 **Lead Architect (Model: Pro)**:
   - Проектирует архитектуру, API контракты и декомпозирует задачи.
   - Сохраняет спецификации в `production_artifacts/Spec.md`.
   - **ОБЯЗАТЕЛЬНО останавливается** для явного подтверждения пользователем перед началом написания кода.

2. 💻 **Builder (Model: Pro)**:
   - Реализует код ядра в `backend/` (FastAPI, OpenCV, EasyOCR/manga-ocr, Pillow) и `frontend/` (Next.js/React).
   - Развивает **единый сервис** `backend/agents/manga_pipeline_service.py`. Запрещено создавать одноразовые `debug_*.py` скрипты в корне backend.
   - Пишет модульные тесты в `tests/`.

3. 🧪 **QA & Auditor (Model: Flash)**:
   - Запускает тесты, проверяет корректность 3-х слоев (`v1_original`, `v2_cleaned`, `v3_translated`).
   - Формирует отчет `production_artifacts/QA_Report.md` и обновляет `README.md`.

---

## 🥋 Доменные правила обработки манги (Memory & Habits)

1. **Клининг и Inpainting (Строгое правило)**:
   - ❌ **ЗАПРЕЩЕНО**: Использовать грубые прямоугольные заливки (`cv2.rectangle`). Это ломает контуры баблов и уничтожает арт.
   - ✅ **ПРАВИЛО**: Использовать маску по пикселям букв (`cv2.threshold`) + `cv2.inpaint(img, mask, radius=3, flags=cv2.INPAINT_TELEA)` или LaMa.
2. **2-Pass OCR Детекция**:
   - Обязательно распознавать как стандартные светлые баблы, так и **инвертированные темные баблы** (белый текст на темном фоне).
3. **Типографика и Тайпсеттинг**:
   - ❌ **ЗАПРЕЩЕНО**: Использовать шрифты без поддержки кириллицы или вставлять IT-теги `*[ ]*` в арт.
   - ✅ **ПРАВИЛО**: Использовать проверенные кириллические TTF-шрифты (`comicbd.ttf`, `arialbd.ttf`, `segoeuib.ttf`).
   - ✅ **ПРАВИЛО**: Динамически масштабировать шрифт под овал бабла и соблюдать контраст (черный текст на белом, белый текст с обводкой на темном).
4. **Интеграция с читалкой Next.js**:
   - Файлы глав должны сохраняться по стандарту: `backend/data/manga/{title}/{chapter}/` с тремя подкаталогами: `v1_original`, `v2_cleaned`, `v3_translated`.

---

## 💰 Квотная гигиена (AI Pro $20)
- Задачи на анализ и кодинг -> `Pro`.
- Запуск тестов, проверка логов, документация -> `Flash`.
- Запрещено передавать в контекст чата сырые OCR-массивы на тысячи строк — сохраняйте их в JSON-кэш на диске.

---

## 🚀 Git & Deployment Hygiene (Строгое правило)
- ✅ **ОБЯЗАТЕЛЬНО**: После каждого завершенного изменения, фикса или выполнения этапа — **всегда автоматически пушить все изменения в Git репозиторий (`git add .`, `git commit`, `git push`)**. Не ждать отдельного запроса от пользователя.

