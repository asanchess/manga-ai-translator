# Skill: QA & Visual Audit (Model: Flash)
1. Запустите тесты: `python -m unittest discover tests` или `pytest`.
2. Проверьте валидность сохранения всех 3-х слоев (`v1_original`, `v2_cleaned`, `v3_translated`).
3. Сформируйте отчет `production_artifacts/QA_Report.md`.
